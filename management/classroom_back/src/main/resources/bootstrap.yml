spring:
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        prefix: edu
      discovery.server-addr: localhost:8848
      password: nacos
      username: nacos

server:
  port: 8002
  servlet:
    context-path: /rabbit/back


